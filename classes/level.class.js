class Level {
    enemys;
    clouds;
    backgroundObjects;
    levelLength;

    constructor(enemys, clouds, backgroundObjects, levelLength) {
        this.enemys = enemys;
        this.clouds = clouds;
        this.backgroundObjects = backgroundObjects;
        this.levelLength = levelLength;
    }
}